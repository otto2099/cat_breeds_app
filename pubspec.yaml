name: cat_breeds_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  bloc: ^9.0.0
  cupertino_icons: ^1.0.6
  dartz: ^0.10.1
  dio: ^5.8.0+1
  equatable: ^2.0.7
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.0
  flutter_dotenv: ^5.2.1
  flutter_localizations:
    sdk: flutter
  get_it: ^8.0.3
  go_router: ^15.1.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  mocktail: ^1.0.4
  shimmer: ^3.0.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1
    


flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/logo_thecatapi.png"

flutter:
  uses-material-design: true

  assets:
    - assets/
    - .env
